# ADR 005: Preserve source provenance

Status: Accepted (2026-08-25).

Canonical state alone cannot answer which source reported a value or why it changed. Snapshots, observations, source identities, and attribute/relationship claims are retained under policy. Reconciliation produces effective state without overwriting evidence. Storage grows with history, so retention is configurable and raw artifacts are optional/short-lived.

